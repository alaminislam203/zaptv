'use client';

// This is the main entry point for the Next.js app router.
// It imports and renders the FalconSports component from the old index.tsx file.

import FalconSports from '@/app/index';

export default function Page() {
  return <FalconSports />;
}
